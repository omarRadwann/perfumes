# Optional — only needed if you run the Tripo image-to-3D pipeline (npm run models).
# The shipped website never uses this; the flacons are crafted in real WebGL.
# Get a key at https://platform.tripo3d.ai  → copy this file to .env and fill it in.
TRIPO_API_KEY=
